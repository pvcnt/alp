To package the project: make

The jar will be created in dist. To run the project:

java -jar dist/privamov-runtime-bin.jar make ./etc/accio/srds16-geoind.json

Commands available:
  * make
  * summarize